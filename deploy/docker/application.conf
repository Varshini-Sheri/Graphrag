neo4j {
  uri = "neo4j+s://76aeed1c.databases.neo4j.io"
  user = "neo4j"
  password = "6pOf3FuN-jmGWdwUe-y1LPNJSuE6iohnYNAcUqupsNg"
  database = "neo4j"

  # Recommended Aura driver settings
  maxConnectionPoolSize = 50
  connectionTimeout = 30s
  connectionAcquisitionTimeout = 60s
  maxTransactionRetryTime = 15s
}

ollama {
  endpoint = "http://ollama:11434"
  model = "llama3:instruct"
  temperature = 0.3
  timeoutMs = 120000
  maxRetries = 3
}

index {
  documents = "/app/data/index/documents.jsonl"
  chunks    = "/app/data/index/chunks.jsonl"
  format = "jsonl"
  batchSize = 100
}

flink {
  parallelism = 1
  checkpointInterval = 60000
  checkpointDir = "file:///tmp/flink-checkpoints"
}
